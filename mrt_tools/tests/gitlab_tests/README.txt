To run the gitlab tests, you need to run a local test instance of Github. To do so run "docker-compose up" in this
directory. Once Gitlab has started go to "http://localhost:10080" and set the root password to "gitlab_root".

